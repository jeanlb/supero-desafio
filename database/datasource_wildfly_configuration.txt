- Configurar conector MySQL no Wildfly:

1 – Acesse o diretório $WILDFLY_HOME/modules/system/layers/base/com.
2 – Uma vez lá, crie o diretório mysql e seu subdiretório main.
3 – Dentro de main coloque o jar do driver JDBC do MySQL.
4 – Agora crie um arquivo chamado module.xml e dentro dele adicione as seguintes linhas de código:

<?xml version="1.0" encoding="UTF-8"?>
 
<module xmlns="urn:jboss:module:1.1" name="com.mysql">
    <resources>
        <!-- Nomear o conector segundo com o que for utilizado (versao) -->
        <resource-root path="mysql-connector-java-5.1.47.jar"/>
    </resources>
    <dependencies>
        <module name="javax.api"/>
        <module name="javax.transaction.api"/>
        <module name="javax.servlet.api" optional="true"/>
    </dependencies>
</module>

5 – Agora acesse o diretório $WILDFLY_HOME/standalone/configuration e abra o arquivo standalone.xml.
Exemplo de configuração de datasource, a ser adicionado no arquivo standalone.xml do Wildfly, entre as tags <datasources> desse arquivo.

<datasource jta="true" jndi-name="java:jboss/datasources/db_task-ds" pool-name="db_task" enabled="true" use-java-context="true" use-ccm="true">
		    <connection-url>jdbc:mysql://localhost:3306/db_task</connection-url>
		    <driver>mysql</driver>
		    <transaction-isolation>TRANSACTION_READ_COMMITTED</transaction-isolation>
		    <pool>
		    	<!-- Calculo padrao para connection pool: pool minimo/inicial sendo a quantidade de cores do servidor; 
		    	     pool maximo sendo a quantidade de cores x2 -->
		        <min-pool-size>4</min-pool-size>
		        <max-pool-size>8</max-pool-size>
                <prefill>true</prefill>
		    </pool>
		    <security>
		        <user-name>root</user-name>
		        <password></password>
		    </security>
            <timeout>
                <!-- Apos a aplicacao usar uma conexao (concluir alguma operacao CRUD), a conexao eh fechada e devolvida para o pool. 
                	 Ou seja, a conexao eh liberada para outras requisicoes a utilizarem -->
                <!-- indicate the maximum time a connection may be idle before being closed and returned to the pool -->
                <idle-timeout-minutes>0</idle-timeout-minutes>
            </timeout>
		    <statement>
		        <prepared-statement-cache-size>32</prepared-statement-cache-size>
		        <share-prepared-statements>true</share-prepared-statements>
		    </statement>
</datasource>

- Exemplo de configuração de driver do datasource acima, para ser adicionado entre as tags <drivers>

<driver name="mysql" module="com.mysql">
		    	<xa-datasource-class>com.mysql.jdbc.jdbc2.optional.MysqlXADataSource</xa-datasource-class>
 </driver>

- Adapted from: https://desenvolvo.wordpress.com/2013/08/27/configurando-datasource-no-jboss-wildfly-com-mysql/